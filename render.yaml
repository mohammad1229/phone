services:
  - type: web
    name: phone-care-service
    env: node
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 20.11.1
      - key: SUPABASE_URL
        sync: false
        placeholder: "https://your-project-id.supabase.co"
      - key: SUPABASE_KEY
        sync: false
        placeholder: "Your Supabase service_role secret key"
      - key: SUPABASE_BUCKET
        value: phone-care-db
